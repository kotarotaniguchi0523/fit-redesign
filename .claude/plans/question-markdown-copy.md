# Plan: Question Markdown Copy機能

## 概要
各問題にMarkdown形式でコピーできるボタンを追加し、問題文・選択肢・正解・解説を含んだテキストをクリップボードにコピーできるようにする。

## 背景・課題
- 学習者が問題をメモアプリやドキュメントに転記したい場合がある
- 現状は手動でテキストを選択してコピーする必要がある
- Markdown形式で出力することで、Notion、Obsidian、各種メモアプリで綺麗にフォーマットされた状態で貼り付け可能

## 実装ステップ

### Step 1: Markdown変換ユーティリティ関数の作成
`src/utils/questionToMarkdown.ts` を新規作成し、`Question`オブジェクトをMarkdown文字列に変換する関数を実装する。

### Step 2: クリップボードコピーフックの作成
`src/hooks/useClipboard.ts` を新規作成し、クリップボードへのコピー処理とコピー完了状態の管理を行う。

### Step 3: QuestionCardコンポーネントにコピーボタンを追加
既存の`QuestionCard.tsx`にコピーボタンを追加し、クリック時にMarkdownをクリップボードにコピーする機能を実装。

## 作成・変更するファイル

| ファイル | 操作 | 説明 |
|---------|------|------|
| `src/utils/questionToMarkdown.ts` | 新規作成 | Question型をMarkdown文字列に変換する関数 |
| `src/hooks/useClipboard.ts` | 新規作成 | クリップボードコピー処理とUI状態管理のフック |
| `src/components/QuestionCard.tsx` | 変更 | コピーボタンの追加 |

## UIデザインの方針

### ボタンの配置場所
- **場所**: 問題番号の右側、タイマーの左側（既存のヘッダーエリア内）
- **理由**:
  - 問題単位の操作なので問題番号近くが直感的
  - タイマーと同じ行に配置することでUIの一貫性を保つ

```
┌─────────────────────────────────────────────┐
│ [1] 問題文テキスト...        [📋] [タイマー] │
│                                             │
│ 選択肢...                                    │
└─────────────────────────────────────────────┘
```

### ボタンのスタイル
- HeroUIの`Button`コンポーネントを使用
- `size="sm"`, `variant="light"` で控えめなスタイル
- アイコンのみ（テキストなし）でコンパクトに
- ホバー時にツールチップで「Markdownでコピー」を表示
- コピー成功時: アイコンをチェックマークに変更し、2秒後に元に戻す

### アイコン
- 通常状態: クリップボードアイコン
- 成功状態: チェックマークアイコン
- 色: `text-slate-500` → ホバー時 `text-[#1e3a5f]`

## Markdownフォーマットの例

### 基本形式（選択肢なし）
```markdown
## 問題 1

A3.C₍₁₆₎を10進数で表せ

### 解答
163.75

### 解説
16進数A3.Cを10進数に変換。整数部: A×16 + 3×1 = 10×16 + 3 = 163。小数部: C/16 = 12/16 = 0.75。よって163.75₍₁₀₎
```

### 選択肢ありの形式
```markdown
## 問題 5

次の10進少数のうち、2進数で表すと無限小数になるものを全て選べ

### 選択肢
- **ア**: 0.05
- **イ**: 0.125
- **ウ**: 0.3125
- **エ**: 0.4

### 解答
ア，エ

### 解説
2進数で有限小数になるのは、分母が2のべき乗で表せる分数のみ...
```

### 図がある場合
```markdown
## 問題 3

次の状態遷移図について答えよ

> **図**: 状態遷移図（S0, S1, S2の3状態、入力0,1による遷移）

### 解答
イ

### 解説
（解説文）
```

## 検証方法

1. **基本機能の確認**
   - 各問題のコピーボタンをクリックしてクリップボードにコピーされることを確認
   - コピーしたMarkdownをテキストエディタに貼り付けて形式を確認

2. **UI/UXの確認**
   - コピー成功時にアイコンがチェックマークに変わることを確認
   - 2秒後にアイコンが元に戻ることを確認

3. **エッジケースの確認**
   - 選択肢がない問題でも正しく動作するか
   - 解説がない問題でも正しく動作するか
   - 図の説明がある問題でも正しく動作するか

## 技術的な補足

### useClipboardフックの設計
```typescript
interface UseClipboardReturn {
  copy: (text: string) => Promise<void>;
  isCopied: boolean; // コピー成功後2秒間true
}
```
